# Thanawyiah 🎓 — عقد Lovable التنفيذي

## الهدف

ابنِ تطبيق ويب إنتاجي باسم **Thanawyiah 🎓** لطلاب الثانوية والبكالوريا العربية. يحوّل الطالب مصادر التعلم إلى ملزمة، ملخص، وكويز تفاعلي قابل للطباعة والتصدير. النموذج الحالي في `index.html` هو المرجع السلوكي والبصري، وليس بنية الإنتاج النهائية.

## قاعدة العمل الأساسية

استخدم React + TypeScript + Vite + Tailwind + shadcn/ui في الواجهة، وSupabase للمصادقة والقاعدة والتخزين وEdge Functions. لا تنقل أي مفتاح API إلى الواجهة. تعامل مع `api.config.example.js` كمرجع نماذج محلي فقط، لا كبنية إنتاجية.

## الوظائف التي يجب الحفاظ عليها

| المجال | المتطلب |
|---|---|
| المصادر | لصق نص، رفع PDF، رفع صور، ملف HTML، ورابط YouTube مع إدخال نص يدوي احتياطي |
| التحليل | اكتشاف اللغة ونوع المحتوى والمهارات وجودة الاستخراج قبل التوليد |
| الكويز | MCQ مع إجابة صحيحة وشرح ودليل من المصدر و`skill_area` و`cognitive_level` وجودة رقمية |
| الملازم | أهداف درس، شرح مفهوم، أمثلة، أخطاء شائعة، مراجعة سريعة، بنك أسئلة مع إجابات وشروح |
| الإنجليزية | Vocabulary، Grammar، Word Formation، Reading، ومهارات مختلطة بلا ترجمة عامة للمصدر |
| التصدير | طباعة A4، PDF من المتصفح، وتنزيل HTML وJSON للكويز |
| المكتبة | حفظ المصادر والملازم والاختبارات ومحاولات الطالب داخل حسابه |

## خارطة الشاشات

1. **Onboarding**: اختيار البلد والمنهج والصف والشعبة واللغة المفضلة.
2. **Dashboard**: بطاقة «تابع درسك»، مؤشرات تقدم حقيقية من المحاولات، وآخر عناصر المكتبة.
3. **Create**: إدخال المصدر ثم تحليل المحتوى ثم اختيار نوع الناتج وإعداداته.
4. **Quiz Player**: مؤقت اختياري، شريط تقدم، تصحيح فوري أو مؤجل، وشروح بعد الحل.
5. **Study Guide**: عرض الملزمة، فهرس، وضع تركيز، وطباعة منظمة.
6. **Library**: فلاتر المادة والصف والنوع واللغة، مع حفظ وتنزيل وحذف من حساب المستخدم.
7. **Profile & Settings**: المنهج، التفضيلات، وإدارة الحساب؛ لا تعرض مفاتيح الذكاء الاصطناعي هنا.

## ترتيب البناء

ابدأ بالتصميم وعقد البيانات، ثم المصادقة والمكتبة، ثم رفع المصادر وتحليلها، ثم Edge Function للتوليد، ثم مشغل الكويز والملزمة، ثم التصدير والاختبارات. اعمل في فروع صغيرة أو مكونات منفصلة، ولا تعِد كتابة واجهة النموذج كملف HTML واحد.

## معايير القبول

يجب أن ينشئ المستخدم حسابًا، يرفع مصدرًا، يطلب كويزًا، يحلّه، يرى إجابة مفسرة، ثم يجد المحاولة والكويز في مكتبته. يجب أن يعمل المسار على الهاتف وسطح المكتب، وأن يدعم العربية RTL والإنجليزية LTR داخل المحتوى نفسه. يجب أن تتولى Edge Function فقط نداءات النماذج وأن تسجّل الحد الأدنى من القياسات دون نص المصدر أو المفاتيح.

## الهوية

الهوية هي **أطلس المذاكرة**: نيلي عميق للثقة، ذهبي أطلسي للإنجاز، ورق كريمي للمحتوى، ومسار درس مرقم يربط المصدر → التحليل → الملزمة → الكويز → المراجعة. لا تستخدم تدرجات بنفسجية أو واجهة SaaS مركزية عامة أو خط Inter كخط وحيد.
